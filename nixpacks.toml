[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --force"]

[phases.build]
cmds = ["vite build"]

[start]
cmd = "npm run preview"   # or "vite preview --host 0.0.0.0 --port 3000"